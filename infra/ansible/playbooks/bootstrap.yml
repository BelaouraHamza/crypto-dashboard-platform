- name: Bootstrap local Kubernetes cluster with TLS and ArgoCD
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    use_ci: "{{ lookup('env','GITHUB_ACTIONS') | default(false) }}"
  roles:
    - tls
    - cluster
    - deploy
